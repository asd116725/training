# 健身力量训练与碳循环减脂工具计划

## 概要

- 构建一个本地全栈 Web 工具：前端放在 `frondend/`，后端放在 `java/`，数据库使用本地 MySQL。
- 第一版支持个人信息录入、目标体脂计算、每日热量/三大营养素推荐、碳循环日类型、五餐餐食记录、食材库维护、剩余餐食推荐。
- DeepSeek 第一版做后端预留接口：有 API Key 时调用 DeepSeek，无 API Key 时返回规则推荐，保证初学阶段项目可运行。

## 技术方案

- 前端：使用最新 Vite + React + TypeScript 脚手架；本机用 `nvm use 20.19.5` 运行，满足最新 Vite 对 Node 版本的要求。
- 前端界面：做成训练营操作台，而不是营销页；首屏包含个人档案、目标体脂、今日碳循环、热量表、五餐录入、食材库、AI 推荐结果。
- 后端：使用 Spring Boot 3 + Java 17 + Maven Wrapper，适合 Java 初学者；分层为 Controller、Service、Repository、DTO、Entity。
- 数据库：MySQL 本地库，提供初始化 SQL；后端默认使用环境变量配置连接，文档给出本地示例。
- 文档：外层 `README.md` 写总架构；`frondend/README.md` 写前端启动、页面结构和开发说明；`java/README.md` 写后端启动、接口、数据库和 DeepSeek 配置。

## 核心功能与接口

- 个人档案：
  - 字段：性别、身高、体重、年龄、当前体脂率、目标体脂率、训练频率、活动水平。
  - 后端提供保存、查询、计算推荐接口。
- 饮食计算：
  - 使用 Mifflin-St Jeor 估算 BMR，结合活动系数得到 TDEE。
  - 按减脂目标生成每日热量赤字建议。
  - 碳循环分为高碳日、中碳日、低碳日；力量训练日默认高/中碳，休息日默认低碳。
  - 输出每日总热量、蛋白质、碳水、脂肪目标，以及每餐建议区间。
- 食材库：
  - 字段：食材名称、每 100 克蛋白质、碳水、脂肪、热量大卡。
  - 支持新增、修改、删除、列表查询。
- 餐食记录：
  - 每日分为早餐、午餐、练前餐、练后餐、晚餐。
  - 用户可选择食材和克数录入已吃内容。
  - 系统汇总今日已摄入，并计算剩余热量和三大营养素缺口。
- 推荐接口：
  - 输入：个人目标、今日碳循环类型、已吃餐食、未完成餐次、食材库。
  - 输出：推荐未吃餐次、食材组合、克数、总热量、三大营养素、理由。
  - DeepSeek 通过后端环境变量 `DEEPSEEK_API_KEY`、`DEEPSEEK_BASE_URL`、`DEEPSEEK_MODEL` 配置；未配置时使用本地规则推荐。

## 数据库设计

- `user_profiles`：个人信息与目标。
- `foods`：食材库。
- `meal_logs`：每日餐次记录。
- `meal_log_items`：餐次中的食材明细。
- `recommendation_records`：AI 或规则推荐结果记录，便于复盘。
- 初始化脚本放在后端资源目录，包含建库、建表和基础示例食材。

## 测试与验收

- 前端：
  - 能录入个人信息并展示每日推荐热量表。
  - 能维护食材库。
  - 能按五餐录入食材并实时看到剩余摄入缺口。
  - 能点击推荐按钮并展示规则/DeepSeek 推荐结果。
  - 执行 `npm run build` 通过。
- 后端：
  - 单元测试覆盖饮食计算、碳循环目标、规则推荐。
  - 接口测试覆盖个人档案、食材库、餐食记录、推荐接口。
  - 执行 Maven Wrapper 测试通过。
- 数据库：
  - 初始化 SQL 可在本地 MySQL 执行。
  - 后端启动后可正常连接并完成 CRUD。
- 文档：
  - 初学者可按 README 从零启动前端、后端和数据库。

## 假设与默认选择

- 目录名按你的要求保留为 `frondend/`，不自动改成 `frontend/`。
- 第一版不做登录注册，只做单用户本地工具。
- 第一版不做医疗级建议，文档中注明计算结果仅作健身饮食规划参考。
- DeepSeek 先预留并可降级为规则推荐，避免 API Key 缺失导致项目不可用。
- 后端使用 Maven Wrapper，因为本机当前没有全局 `mvn`。
